stages:
  - build
  - test

image: gcc

build-client:
  stage: build
  before_script:
    - apt update && apt -y install make
  script:
    - make client-build
  artifacts:
    paths:
      - ./client/obj
      - ./client/bin
  only:
    changes:
      - client/**/*

build-serveur:
  stage: build
  before_script:
    - apt update && apt -y install make
  script:
    - make serveur-build
  artifacts:
    paths:
      - ./serveur/obj
      - ./serveur/bin
  only:
    changes:
      - serveur/**/*

test-client:
  stage: test
  needs: ["build-client"]
  script:
    - cd client
    - ./test/check.sh